# 个人工作台

单用户个人工作台：错题学习（拍照录入 → OCR 识别 → 分类归档 → 随机抽题复习 → 六级掌握度评估 → PDF 导出试卷）+ 个人管理（仪表盘总览 / 待办 / 目标 / 收件箱 / 日历 / 复盘 / 热点资讯 / 收藏夹）。

## 版本

- 当前版本：**v1.25.1**
- 版本规则见 `AGENTS.md`：每次修改代码必须升级版本号并推送到 GitHub。

## 当前能力（v1.25.1）

- v1.25.1（修复）：**上传目录（`UPLOAD_DIR`）跟随数据库所在目录**——测试用临时数据库时上传文件自动隔离，`/api/reset` 不再误清真实上传资料（真实部署默认仍为项目目录 `uploads/`）。

- v1.25.0（全面排查修复 · 阿里云部署准备）：**服务端部署与安全**——新增 `HOST`（默认 127.0.0.1，公网直连设 0.0.0.0）、`ALLOW_REGISTER=0`（关闭开放注册）、`DISABLE_DEMO_ACCOUNT=1`（不再自动创建 admin/admin123 演示账号，公网必配）、`COOKIE_SECURE=1`（HTTPS 反代）、`OCR_ENGINE`（real/mock/off：Linux 无 MinerU 时识别明确报错，**绝不把模拟文本当真结果入库**）、`MINERU_CLI`（Linux 可指向 mineru 二进制）、`BACKUP_DIR`；静态文件修复路径穿越（`..` 段拒绝 + 边界校验）、禁止下载服务端源码/配置/备份、**`/uploads/` 个人资料需登录后访问**；Cookie `Max-Age` 修正为相对秒数（原误用 epoch 秒导致 55 年不过期）；备份文件名改本地时区（原 UTC 早晨差一天）；写队列防毒化（单请求失败不再导致后续所有写入静默丢失）；`reviewSeq` 改为按最大 id 计数（删除题目后新复习记录不再主键冲突）；`qidSeq` 用 reduce 防大数组栈溢出；全部 JSON 列解析加容错（脏数据不再整库 500）；恢复改为临时文件原子替换 + 失败自动重开连接 + 清理旧 WAL；端口占用友好提示、SIGTERM/SIGINT 优雅关闭、PDF 导出支持 Linux 浏览器路径。**前端**——题库列表知识点列补 `esc()`（修复存储型 XSS）；推荐算法修复无错因标签题目得分 `-Infinity` 导致被饿死；进行中的复习/背单词会话不再被导航往返与跨标签同步强制重置；单词会话切页后不再劫持仪表盘快捷键；`quickRate` 加急/薄弱标记补持久化；断点续传对已删除题目重算下标；子任务/里程碑 id 计数随数据恢复（刷新后勾选/删除不再错位）；删除知识点只移除该知识点（不再清空题目全部知识点）；内置词书章节禁止删除（防重导同 id 重复题）；覆盖导入后重算个人数据 id 计数器；背单词统计全部改日志索引（3000 词量级不再卡顿数秒）、「今日到期」与复习按钮口径统一（已掌握不计）、每日新词上限按日累计（重复点击不再突破）、日历到期分布预计算；版本号/文档/测试基建同步（verify-review-algo 补真实断言、api-test 修复假绿、verify-v19 适配乱序、CI 补齐 3 个测试、README 新增 Linux 云端部署章节）。
- v1.24.0（背单词 学习/复习分离 + 改良艾宾浩斯 + 乱序）：**「开始学习」与「开始复习」两个独立按钮**——📖 学习 = 今日新词（每日上限，**乱序**）；🔁 复习 = 今日到期词（**已掌握的不再出现**，按到期紧急度分组、同日乱序）。**改良艾宾浩斯遗忘曲线**——认识/模糊/不会三档反馈各自推进下次复习间隔（认识按难度系数递增 1→3→8→20 天；模糊间隔减半且难度 −0.1；不会重置为 1 天且难度 −0.2、记一次重学），**连续答对 3 次判定「完全掌握」，移出复习队列**不再打扰；学习/复习/困难单词三种会话小结标题区分。**统计口径统一**——「今日到期」与「开始复习（N）」按钮数字一致（已掌握词不计到期）。**队列乱序**——新词学习、困难单词本复习均洗牌，不再按字母/导入顺序。
- v1.23.0（背单词下拉子菜单 + 分功能页）：**背单词功能拆分为下拉子菜单**——侧边栏「🎴 背单词」点击展开子菜单（桌面侧边栏与移动端「更多」抽屉同步）：**📖 开始学习**（词书进度 + 开始按钮 + 发音开关）/ **📊 学习记录**（今日新词/复习/到期/打卡四格 + 近 14 天每日明细 + 单词状态筛选）/ **⭐ 困难单词本**（收藏列表 + 一键复习全部收藏词）/ **🔍 查单词**（按单词/中文释义/英释全局搜索，可发音、收藏、单独学习）；页面顶部同步提供功能 tab 条，一次只显示一个功能页，不再一屏堆叠。**学习卡放大居中**——学习卡最大宽 820px 垂直居中、单词放大到 58px，占满页面视觉中心。**「下一个」连续过词**——卡片正面/背面都有「下一个 ›」按钮（记认识并前进），支持 N / → 快捷键，背单词不再每词多点好几下。当前功能页与发音开关等设置随账号持久化。
- v1.22.1（修复）：服务启动横幅版本号改为从 `VERSION` 文件读取（此前硬编码旧版本号导致横幅与文件不一致）。
- v1.22.0（背单词重构 · 简化卡片流）：**核心学习流程简化为单一卡片流**——砍掉例句猜义（先句子后单词）、听写、看词选义/看义选词等全部模式，全程只做卡片判断：看单词+音标 → 空格翻卡（中文释义/例句/英释/双音标/近义词/同根词）→ 认识/模糊/不会三档自评，结果驱动 SM-2 间隔重复，模糊与不会自动回炉；每日队列 = 到期复习优先 + 新词补足上限不变。**困难单词本（收藏）**——任意单词可一键加入/移除收藏（卡片翻卡后 / 状态列表 / 搜索 / 详情弹窗均可操作），收藏词仍参与正常复习计划，可一键「⭐ 复习困难单词」单独过一遍全部收藏词。**数据记录面板**——系统自动后台记录（无需打卡）：今日新词数 / 今日复习数 / 今日到期 / 连续打卡天数四格统计；近 14 天每日新词量与复习量明细表；单词状态筛选（全部 / 待复习 / 未学 / 已掌握 / 困难）列表，每词显示掌握等级、到期时间，支持发音与收藏。**单词全局搜索**——词书页顶部搜索框按单词/中文释义/英释即时过滤（快捷键 `/` 聚焦），命中项可发音、收藏、一键单独学习。**发音开关**——设置页与词书页可关闭/开启 TTS（新词首刷自动朗读一次）。学习进度照旧存本地 SQLite（云端就绪架构不变）。
- v1.21.0（背单词独立页面 + 错题流净化）：**背单词从仪表盘提取为侧边栏独立页面「🎴 背单词」**——侧边栏「学习」组新增入口（移动端「更多」抽屉同步），独立页面包含：词书进度 + 四种学习模式选择 + 开始按钮的**入口面板**（此前模式选择面板从未显示，属 v1.20 遗留缺陷）、单词卡、本轮小结，仪表盘「🎴 背单词」按钮与设置页「🎴 去背单词」一键跳转，会话退出/完成回到词书页而非仪表盘；**错题复习流排除词汇类**——仪表盘「今日到期」、推荐复习、复习到期题、按配置抽题、薄弱知识点、掌握度/错因图表与日历到期角标不再统计 3000 个单词（此前单词混入错题队列导致「今日到期 3000+ 题」）；**统计口径修复**——「今日到期」只计已学且到期的词（未复习新词不再显示为到期）、「今日新词」如实显示 min(上限, 剩余新词)；**看义选词模式**清除上一词翻卡残留（大字/例句/详情）；快捷键（空格/数字）在背单词页面同样生效。
- v1.20.0（背单词升级 · 对标不背单词）：**词书数据升级**——内置「六级核心词 3000」新增**英英释义 / 英美双音标 / 近义词 / 同根词**（2867/2957/2791/2466 词覆盖，`wordlists/cet6-3000.json`），已导入旧词书可在设置页「重新导入升级版」一键升级（新增 `POST /api/questions/delete-batch` 批量删除）；**例句语境学习**——新词先看英文例句猜词义再翻卡（复习词直接看词）；**单词详情卡**——翻卡后展示英英释义、美/英音标、近义词、同根词、例句；**四种学习模式**——⚡快捷三档 / 🔤看词选义（4 选 1）/ 🔁看义选词 / ✍️听写拼写（TTS 发音 + 回车判定），全部驱动 SM-2 间隔重复、错误自动回炉；**三档统计**——词书进度升级为「已学 X/3000 · ✅认识 🟡模糊 ❌不认识 · 今日到期 N」；模式选择持久化。
- v1.19.0（背单词模式 + 知识点树折叠）：**背单词**——内置「六级核心词 3000」词书（公开词库 hehonghui/dict 提取，含释义/例句/音标，设置页一键导入，自动创建词书章节）；每日队列 = **到期复习词（SM-2 优先）+ 新词补足上限**（默认 50 可调）；单词卡「看词 → 空格翻卡（释义+例句+🔊 发音）→ 认识/模糊/不认识三档自评」，**模糊/不认识自动回炉**本轮再过一遍，整轮小结（认识 X/模糊 Y/不认识 Z）且自评驱动间隔重复；支持**批量粘贴导入**自定义词表（`单词 释义 | 例句`）；仪表盘「🎴 背单词」一键入口 + 词书进度显示；新增 `POST /api/questions/batch` 批量入库接口。**知识点树折叠化**：设置页知识点管理改为科目 → 子科目 → 章节 → 知识点逐级点击展开/收起（默认展开到子科目一层，一屏看全），＋加/改/删按钮不变。新增模块 `js/13-wordbook.js`。
- v1.18.2（学习计时体验重做）：离开页面**不再弹窗询问**——改为自动策略：离开 ≤5 分钟自动计入（查资料/接水不算打断）、超过自动不计，全程静默；设置页新增「⏱ 学习计时」三档策略（自动 / 总是计入 / 不计入），选择持久化；页面隐藏时停止逐秒计时（避免与离开补记重复），语义统一为"计时 = 可见时间 + 策略允许的离开时间"。
- v1.18.1（缺陷修复）：JSON 备份补齐 v1.18 新增字段（examDate / moduleOn / habits）；覆盖导入完整替换扩展状态（复习集/习惯/考试日期/模块开关/主题/断点，缺省回退默认值，不再残留旧值）；登录路径（doLogin）现在也按模块开关刷新导航；新增 `verify-sweep.mjs` 全页面走查（13 视图 + 关键交互 + 异常监控）；拆分完整性静态验证（拆分前 340 个顶层符号 0 缺失 0 重复）。
- v1.18.0（考研冲刺全家桶 + 工程地基）：**考研倒计时**（设置页填考试日期，仪表盘横幅显示剩余天数，≤60 天自动进入**冲刺模式**提示）；**每日习惯打卡**（仪表盘习惯卡，🔥 累计天数徽章，随账号落库）；**OCR 真异步化**（服务端任务队列串行识别 + 前端轮询进度，批量录入不再阻塞页面）；**日历到期分布**（每月格显示 🔥 当日到期复习题数）；**复习卡键盘快捷键**（空格翻答案 · 1/2/3/4 四档自评 · S 跳过 · ←/→ 切题）；**词汇 TTS 发音**（浏览器原生语音，复习卡与详情页 🔊）；**遗忘曲线**（详情页累计掌握分时间线）；**题库批量删除 / 批量导出**；**试卷难度配比**（错误轨道题占比滑块）；**今日学习计划**（到期复习 → 薄弱知识点攻克 → 录入新题三步清单）；**学情周报 Markdown 导出**；**外围模块开关**（热点资讯/收藏夹可隐藏，设置页持久化）；**测试基建** `test-helper.mjs` + **API 直测** `api-test.mjs`（28 项）+ **掌握度算法专项** `verify-mastery.mjs`（10 项）；题库去重改 7 天窗口预过滤（性能优化）；bookmarks 补 PUT 端点。
- v1.17.0（代码结构重构）：业务逻辑由单文件 `app.js`（4600+ 行）拆分为 **12 个按域模块**（`js/01-core` ~ `js/12-boot`），经典 script 顺序加载，**功能与测试零改动**；版本校验脚本同步更新。
- v1.16.0（章节体系 + 复习区融合）：**知识点树补全**——按考研数学考纲与 408 官方大纲，数学（高数 6 章 / 线代 6 章 / 概率统计 6 章）与 408（数据结构 7 章 / 组成原理 7 章 / 操作系统 5 章 / 网络 6 章）共 **46 章 90 知识点**；老库启动时自动升级知识点树（题目数据不受影响）。**仪表盘复习区融合**：原「今日推荐复习」+「随机复习」+「复习到期题」三个入口合并为一张「🎯 复习」主卡（今日任务行 + 推荐预览 + 折叠式抽题配置 + 三个按钮），复习集卡片与最近动态并排。
- v1.15.0（学习动力与效率）：**复习热力图**（ECharts 日历图，近 12 个月每天学习分钟数）+ **连续打卡 streak**；仪表盘「今日复习任务」行（今日到期/已复习/连续打卡 + 一键复习到期题）；复习卡**默写模式**（词汇/作文类先默写再对照答案）；**自建复习集**（题库勾选加入 → 按集一键复习，支持改名/删除，随账号落库）；周月总结新增**学情周报**（周期内薄弱知识点 TOP5 + 错因分布）；全文搜索扩展至笔记/错因；**CSV 批量导入**题目；题库**分页**（每页 100 + 加载更多）与**移动端卡片布局**；**深色模式**（设置页开关，自动保存）；复习断点/提醒日期迁至服务端（消除 localStorage 直读）；**OCR 单题失败重试**按钮；**备份恢复**（设置页上传 .db 一键恢复，服务端校验 + 恢复前自动备份）；服务启动自检横幅（版本/库大小/题数/备份/上传文件数）；关键输入框补 aria-label。
- v1.14.0（架构与学习功能）：全部写操作改为增量 API（题目/复习记录/待办/目标/复盘/收件箱/收藏/设置/知识点树），仅导入/覆盖/重置走整库快照；多标签页打开时自动同步数据（BroadcastChannel）；**错题原图落库**（拍照/粘贴的原图存 uploads/，详情页与复习卡可查看）；**数据库备份**：设置页一键下载整库快照（VACUUM INTO），服务启动自动备份（backups/，保留 7 份）；删除收藏连带清理上传文件；**间隔重复调度（SM-2 轻量版）**：按复习记录推导到期时间，推荐与抽题到期优先，详情页显示下次复习时间；复习配置新增**错因标签专项筛选**；复习卡可先「回忆错因」再看答案；「重置演示数据」由服务端重播种子（数据源统一 seed-data.js）；新增 `package.json`（engines ≥ Node 22.5 + npm scripts）与 GitHub Actions CI（版本校验 + 冒烟/布局/注册/抽题/调度测试）；`scripts/check-version.mjs` 校验版本号单一来源。
- v1.13.3 修复（安全加固）：畸形 URL（如 `/%zz`）不再导致服务崩溃；除登录/注册/会话查询/试卷 HTML 外的全部 API 强制 Cookie 会话鉴权（未登录返回 401）；移除 CORS 通配头（任意网页不再能跨站读写本机数据库）；静态文件禁止下载 SQLite 数据库 / `.git` / `node_modules`；过期会话每小时自动清理；登录成功后才加载数据。
- v1.13.2 修复：批量归类全链路可用、知识点导航筛选生效、横向树布局、补齐 questions/ocr-status 接口。
- v1.13.1 修复：快速添加周几日期（周X/下周X/下下周X）精确换算、非优先级 `!` 内容不再被吞、AI 日报正常渲染、移动端新增「更多」抽屉补齐全部页面入口、JSON 备份/导入/覆盖包含个人数据、重置演示数据清空个人数据、收藏上传类型白名单、热点话题时间与中文分类、演示通知真实数字等。

## v1.13.2 修复

- 批量归类全链路可用：可同时设置科目 / 子科目 / 章节 / 知识点（多选），未动的项保持原值，支持一键设为未分类。
- 知识点导航筛选真正生效（此前点击树节点不过滤）；布局从左侧竖排树改为顶部横向「科目 → 子科目 → 章节 → 知识点」逐级筛选条。
- 补齐接口：`GET /api/questions`（题库列表）、`GET /api/ocr/status`（OCR 轮询占位）。

- 侧边栏分组：总览 / 计划与复盘 / 学习 / 资讯与资料 / 系统；错题功能归入「学习」，健康模块已移除。
- 仪表盘总览（重排）：问候语 → 快捷入口 → 今日概览卡（学习 / 录入 / 复习 / 待办完成率）→ 目标进度 + 今日待办 → 最近动态 + 今日推荐复习（含未掌握 / 总题数 / 错误轨道 / 累计时长迷你指标）→ 随机复习 → 数据统计。
- 今日待办：快速添加解析（今天 / 周X / 下周一自动算日期，#标签，!高/中/低 优先级）、子任务、优先级、标签、提醒日期、备注、编辑 / 删除、列表 / 看板双视图。
- 目标与规划：状态分组（进行中 / 已完成 / 已搁置）、里程碑清单（勾选自动推进度）、挂关联待办（按完成率自动算进度）、目标日期倒数日。
- 收件箱：随手记（支持 #标签），一键转待办 / 目标 / 复盘素材，归档 / 恢复 / 删除。
- 日历视图：月视图统一展示待办截止、目标节点、复盘记录与每日学习时长，点日期看详情。
- 今日复盘：保存自动附带当天数据（学习时长 / 录入 / 复习 / 待办完成率），历史复盘显示当日数据，新增本周复盘汇总（复盘天数 / 平均心情 / 学习录入复习 / 高频卡点）。
- 周月总结：本周 / 本月一键切换，聚合录入题数、复习次数、学习时长、待办完成率、复盘天数；新增心情趋势折线图（最近 14 天）。
- 随机复习：抽题配置升级为 科目（数学 / 英语 / 408）→ 子科目（高等数学…）→ 章节 三级联动筛选。
- PDF 导出试卷：题库页「导出试卷」，按科目 / 子科目 / 章节 / 掌握度筛选，随机抽题（默认 12 道），服务端用本机 Edge / Chrome 无头模式生成 PDF（KaTeX 公式渲染，含姓名日期抬头，可选附带答案解析页）。
- 热点资讯：接入 AI HOT 匿名 API（服务端代理 + 60 秒缓存），过去 24 小时 / 最近一周 / 最热话题 / AI 日报四个 Tab，标题跳转原文，页面底部标注数据来源。
- 收藏夹：收藏链接、上传 PDF / 文件（存本地 uploads/）、笔记备忘，支持标签、备注、类型筛选、打开 / 删除。
- 视觉：Notion 风格（浅色暖白 #F7F6F3、细边框、小圆角、轻阴影、蓝色强调 #2383E2）。

- 统一「识别录入」：单题（1 张图）与批量（多张图，题目/解题标记 + 配对）合为一个入口。
- 真实登录：用户名/密码存 SQLite（scrypt 加盐哈希），Cookie 会话自动登录（7 天），支持注册；演示账号 admin/admin123。
- 修复：今日推荐复习按钮现在直接用推荐列表抽题（之前误走随机抽题）；复习章节选择切页后保留。
- 抽题算法已验证：分层优先保证全覆盖（60 题 × 每轮 3 题 × 20 轮 = 100% 覆盖），验证脚本 `verify-review-algo.mjs`。
- 体验修复：备注框始终可编辑；保存题目后留在录入页继续录（成功/失败都有提示）；题库与详情页公式统一 KaTeX 渲染；选择题选项（A. B. C. D.）自动分行；导入 JSON 缺字段自动补全。
- 视觉改版：Notion 风格登录页与全站组件（浅色暖白 + 细边框 + 小圆角 + 轻阴影 + 蓝色强调）。
- 录入页拆分：题目图片 / 解题过程图片两个上传区（各自多张、相册/拍照/粘贴）；每道题可点「该题无过程」只识别题面；自动配对 + 点选配对。
- MinerU 真实识别：服务端调用本机 `mineru-open-api`（token 读取 `~/.mineru/config.yaml`），pipeline 模型 + OCR + 公式，失败自动回退 flash-extract；设置页可测试并查看耗时。
- 修复：粘贴图片按所选上传区归属（题目/过程）、原图区分「题目原图/解题原图」两块对照、识别结果严格进对应框（题面/解题/备注不串）、MinerU 输出的 LaTeX 自动预处理并用 KaTeX 渲染预览。
- 体验：题目+过程各传一张自动配对识别（不用手动点配对）；识别结果默认只显示渲染后的公式，源码折叠可随时「✏️ 编辑源码」；原图对照左右并排缩小。
- 数据层：本地 SQLite（`server.js` 基于 Node 内置 `node:sqlite`，数据库文件 `mistake-book.db`），页面不再内置测试数据，种子数据在服务端首次启动时写入。
- 仪表盘一体化（已去重）：顶部核心指标（未掌握/总题数/错误轨道/累计时长）→ 今日推荐复习（含前 5 预览）→ 随机复习（抽题配置/做题/自评）→ 数据统计（四张图表，全站唯一图表区）。
- 去重审查：移除重复的指标卡、重复的「今日推荐」入口、重复的掌握度/薄弱知识点/学习时长图表。
- 复习自由选题：抽题后顶部题号导航，可「⏭ 跳过此题」、点任意题号切换、自评后自动跳下一道，小结单独统计跳过题。
- 设置简化：新增科目入口、按钮文案直白（＋加子科目/＋加章节/＋加知识点）、新增/重命名统一弹窗、加分支示例说明。
- OCR 服务：设置页可配置「模拟 / MinerU」，填 Token 后一键连通性测试（记录耗时）；MinerU 走 v4 上传→建任务→轮询→提取文本流程，数据仍存本机。
- 缺陷修复：真实 JSON 导入（合并/覆盖 + ID 重映射）、复习断点续传「继续上次」、学习时长按天落库（真实 30 天曲线）、题目编辑、知识点管理补全（增删改）、复习提醒、OCR 转手动输入。
- 本地持久化：数据存浏览器 localStorage，刷新不丢；首次进入自动播种演示数据。
- API 契约层：`api.js` 定义前后端唯一接口；后端接入只需切 `mode = "remote"`。
- 题库浏览 / 搜索筛选 / 知识点树 / 批量归类、随机复习 + 四档自评 + 六级掌握度、统计图表、JSON 导出导入、学习时长。

## 运行（必须先启动本地服务）

```powershell
双击 start.bat      # 或手动：node server.js
```

然后浏览器打开 http://127.0.0.1:8788（start.bat 会自动打开）。关闭「本地服务」窗口即停止。数据保存在项目目录的 `mistake-book.db`。

## Linux 云端部署（阿里云 ECS 等）

> 应用无任何第三方 npm 依赖（纯 Node 内置模块 + `node:sqlite`），无需 `npm install`。

**1. 安装 Node.js（≥ 22.13.0，22.5~22.12 需 `--experimental-sqlite` 标志，不再支持）**

```bash
# apt 自带的 nodejs 太旧，用 NodeSource 或 nvm 安装 LTS
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs
node -v   # 必须 >= v22.13.0
```

**2. 上传代码并启动（推荐 pm2）**

```bash
# 将项目目录上传到服务器（git clone 或 scp），然后：
cd mistake-book
node server.js                        # 前台测试
# 生产建议用 pm2 常驻：
npm i -g pm2
DB_FILE=/data/mistake-book/mistake-book.db pm2 start server.js --name mistake-book
pm2 save && pm2 startup
```

**3. 环境变量（部署关键配置）**

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `PORT` | 8788 | 监听端口 |
| `HOST` | 127.0.0.1 | 监听地址。**公网直连设 `0.0.0.0`；Nginx 反代保持默认即可（更安全）** |
| `DB_FILE` | 项目目录/mistake-book.db | 数据库文件路径，建议指向数据盘 |
| `BACKUP_DIR` | 项目目录/backups | 每日自动备份目录（保留 7 份） |
| `ALLOW_REGISTER` | 1 | 公网部署**务必设 `0` 关闭开放注册** |
| `DISABLE_DEMO_ACCOUNT` | 未设 | 公网部署**务必设 `1`**：不再自动创建 admin/admin123 演示账号（否则任何人可直接登录） |
| `COOKIE_SECURE` | 未设 | HTTPS 反代场景设 `1`（Cookie 加 Secure 标志） |
| `OCR_ENGINE` | auto | `auto`：有 MinerU 用真实识别，`MINERU_DISABLE=1` 或显式 `mock` 用模拟，否则 **`off`（识别明确报错，绝不把模拟文本当真结果入库）** |
| `MINERU_CLI` | Windows 自动探测 | MinerU CLI 可执行文件路径（Linux 下请装 mineru CLI 并指向它，Windows 用 `.cmd`） |
| `PDF_BROWSER` | 自动探测 | 试卷导出用的无头浏览器路径（Linux 常见路径已内置：chromium / google-chrome） |

```bash
# 公网安全部署示例（pm2 + 环境变量文件）
ALLOW_REGISTER=0 DISABLE_DEMO_ACCOUNT=1 DB_FILE=/data/mistake-book/mistake-book.db \
  pm2 start server.js --name mistake-book
```

**4. Nginx 反代（推荐，HTTPS）**

```nginx
server {
    listen 80;
    server_name your-domain.com;
    # 申请证书后加 443/ssl 配置，并设 COOKIE_SECURE=1
    location / {
        proxy_pass http://127.0.0.1:8788;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

**5. 安全清单（公网必须）**
- `ALLOW_REGISTER=0` + `DISABLE_DEMO_ACCOUNT=1`，并自行用注册接口/数据库创建唯一账号
- 走 HTTPS（Nginx 反代 + 证书），`COOKIE_SECURE=1`
- 云安全组只放行 80/443（反代）或 8788（直连），限制 SSH 来源
- 文件权限：用专用用户 + `umask 077`（pm2/systemd 配置），避免 SQLite/备份/上传文件 0644 全机可读
- OCR：Linux 上若未装 MinerU，识别会明确失败而非返回假文本；可设 `OCR_ENGINE=mock` 仅测试
- 「热点资讯」需要服务器出网访问第三方域名；出站受限时该模块不可用，不影响其他功能
- 重启（pm2 reload）会丢失进行中的 OCR 任务与试卷 token，前端会提示重试，属预期行为

## 测试

```powershell
node smoke-test.mjs     # 功能链路：单题/批量录入、OCR、持久化
node layout-check.mjs   # 桌面/移动布局检查
node feature-check.mjs  # 新功能回归检查
node verify-personal.mjs # 个人工作台功能检查（总览/待办/目标/收件箱/日历/复盘/复习筛选/收藏夹/热点/试卷导出/持久化）
node verify-scheduler.mjs # 间隔重复调度（SM-2）与错因/原图复习功能检查
node verify-v15.mjs       # v1.15.0 新功能检查（热力图/复习集/默写/CSV/分页/深色/卡片）
node verify-v16.mjs       # v1.16.0 章节树与复习区融合检查
node verify-v18.mjs       # v1.18.0 新功能检查（倒计时/习惯/快捷键/TTS/曲线/批量/配比/模块开关）
node verify-v19.mjs       # v1.19.0 背单词检查（折叠树/粘贴导入/单词卡/回炉/每日上限）
node verify-v20.mjs       # v1.20.0 背单词升级检查（详情区/三档统计/批量删除）
node verify-v21.mjs       # v1.21.0 背单词独立页面检查（侧边栏入口/面板可见/复习流排除词汇/快捷键）
node verify-v22.mjs       # v1.22.0 背单词重构检查（单一卡片流/困难单词本/数据面板/搜索/发音开关）
node verify-v23.mjs       # v1.23.0 背单词下拉子菜单检查（子菜单展开/功能页切换/下一个单词）
node verify-v24.mjs       # v1.24.0 学习/复习分离检查（双按钮/掌握移出/遗忘曲线间隔/乱序）
node api-test.mjs         # 服务端 API 直接测试（28 项：鉴权/CRUD/OCR异步/备份恢复/安全）
node verify-mastery.mjs   # 掌握度算法专项测试（六级流转/衰减/边界）
node verify-register.mjs # 注册/登录/登出流程
node verify-review-algo.mjs # 抽题算法覆盖性验证
node browser-shots.mjs chrome   # Chrome 截图 + 跨浏览器验证
node browser-shots.mjs edge     # Edge 截图 + 跨浏览器验证
node browser-shots-personal.mjs chrome  # 个人新页面截图
node scripts/check-version.mjs  # 版本号单一来源校验
```

## 目录

```text
assets/
├── index.html       # 应用入口（含统一识别录入页）
├── js/              # 业务逻辑模块（v1.17.0 起由 app.js 拆分，经典 script 顺序加载）
│   ├── 01-core.js       # 工具函数与全局常量
│   ├── 02-state.js      # 全局状态 + 持久化（apiCall/persistLocal/loadLocal）
│   ├── 03-mastery.js    # 六级掌握度 + SM-2 间隔重复调度
│   ├── 04-auth-nav.js   # 导航 / 登录 / 主题
│   ├── 05-dashboard.js  # 仪表盘 + 推荐算法
│   ├── 06-input.js      # 识别录入（OCR/配对/保存）
│   ├── 07-questions.js  # 题库 / 筛选 / 详情 / 编辑
│   ├── 08-review.js     # 复习流程 + 数据统计图表
│   ├── 09-personal.js   # 个人管理（待办/目标/复盘/日历等）
│   ├── 10-extras.js     # 热点资讯 / 收藏夹 / 试卷导出
│   ├── 11-settings.js   # 设置 / 知识点管理 / 导入导出 / 备份恢复
│   ├── 12-boot.js       # 初始化入口 / window 暴露
│   └── 13-wordbook.js   # 背单词（卡片流/困难单词本/数据面板/搜索/发音）
├── api.js           # API 契约层（local / remote 可切换）
├── server.js        # 本地服务（静态托管 + REST API + SQLite）
├── seed-data.js     # 种子数据（首次启动写入 SQLite）
├── wordlists/       # 内置词书（cet6-3000.json，数据来源 hehonghui/dict，build-wordlist.mjs 可重新生成）
├── start.bat        # 一键启动
├── style.css        # 样式
├── docs/api-contract.md  # 后端接口契约
├── smoke-test.mjs   # 功能冒烟测试
└── layout-check.mjs # 布局检查
```
